// src/types/User.ts

export type User = {
  firstName: string;
  lastName: string;
  address: string;
  email: string;
  policyNumber: string;
  groupNumber: string;
  // Add other fields as necessary
};
